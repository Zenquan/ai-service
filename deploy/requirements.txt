# 部署专用依赖清单（固定版本，保证云端构建可复现）
# 说明：云端向量化走远程 OpenAI 兼容 /embeddings，不打包 fastembed/onnxruntime/本地模型，镜像更小
# Web 框架
fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32

# RAG 核心（rag/requirements.txt 对应项，固定实测版本）
qdrant-client==1.19.0
openai==2.54.0
python-dotenv>=1.0
requests>=2.31
mineru-open-sdk==0.2.5
markitdown==0.1.7

# 客服会话持久化：MySQL 驱动（短连接，云端经 VPC 内网直连）
pymysql==1.2.0

# LangGraph 编排层（客服流程图，0.2.x 与代码 API 匹配）
langgraph==0.2.76
