# 部署专用依赖清单（固定版本 = 本地 rag/.venv 实测通过组合，保证云端构建可复现）
# Web 框架
fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32

# RAG 核心（rag/requirements.txt 对应项，固定实测版本）
qdrant-client==1.19.0
fastembed==0.8.0
openai==2.54.0
python-dotenv>=1.0
requests>=2.31
mineru-open-sdk==0.2.5
markitdown==0.1.7
